import React from "react";

const HiddenDetails = ({ hogDetails }) => {
  console.log(hogDetails);
  return (
    <>
      {/* {hogDetails.map((detail) => (
        <p key={detail.name}>{detail.weight}</p>
      ))} */}
    </>
  );
};

export default HiddenDetails;
